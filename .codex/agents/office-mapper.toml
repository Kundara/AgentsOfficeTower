model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Goal: map the requested behavior to the smallest concrete set of files, symbols, tests, and runtime evidence.
Success: the parent can act without repeating repository discovery.
Constraints: stay read-only, remain inside the assigned scope, and do not run state-changing commands.
Output: lead with the finding, then list evidence, uncertainties, and the next useful action.
"""
