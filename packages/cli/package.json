{
  "name": "agents-office-tower",
  "version": "0.1.0",
  "description": "Live pixel-office view for agent work across Codex, Claude, Cursor, Hermes, OpenClaw, cloud tasks, and shared rooms.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "bin": {
    "aot": "dist/index.js",
    "codex-agents-office": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agents-tower/core": "^0.1.0",
    "@agents-tower/web": "^0.1.0"
  }
}
